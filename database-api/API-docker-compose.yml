
services:
  
  apibox:
    image: "mcr.microsoft.com/azure-databases/data-api-builder:latest"

    restart: on-failure


    volumes:
      - "./startrek.json:/App/dab-config.json"

    ports:
      - "5000:5000"

