query getPlanets($after: String, $first: Int) {
    planets(first: $first, after: $after) {
        endCursor
        hasNextPage
        items {
            PlanetId
            Name
            Coodinates
        }
    }
}